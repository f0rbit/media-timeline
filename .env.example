# Media Timeline - Environment Variables
# Copy this file to .env and fill in your values

# All environment variables are prefixed with MEDIA_ for monorepo compatibility

# Reddit OAuth (required for Reddit integration)
# Create an app at: https://www.reddit.com/prefs/apps
# Set redirect URI to: {MEDIA_API_URL}/api/auth/reddit/callback
MEDIA_REDDIT_CLIENT_ID=
MEDIA_REDDIT_CLIENT_SECRET=

# Twitter/X OAuth (required for Twitter integration)
# Create an app at: https://developer.twitter.com/en/portal/dashboard
# Note: Requires Basic API tier ($100/month) for user tweet access
# Set callback URL to: {MEDIA_API_URL}/api/auth/twitter/callback
MEDIA_TWITTER_CLIENT_ID=
MEDIA_TWITTER_CLIENT_SECRET=

# GitHub OAuth (required for GitHub integration)
# Create an OAuth App at: https://github.com/settings/developers
# Set callback URL to: {MEDIA_API_URL}/api/auth/github/callback
MEDIA_GITHUB_CLIENT_ID=
MEDIA_GITHUB_CLIENT_SECRET=

# URLs
MEDIA_API_URL=http://localhost:8787
MEDIA_FRONTEND_URL=http://localhost:4321

# Devpad API (for authentication)
DEVPAD_API_URL=https://devpad.tools

# Note: EncryptionKey is set via SST secret, not .env
# For production: npx sst secret set EncryptionKey your-32-byte-key
# For local dev: The dev server generates a key automatically
