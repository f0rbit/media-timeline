---

---

<section class="cta-section">
	<h2>Ready to see your story?</h2>
	<p class="description">Updated Live. Free API. Endless possibilities.</p>
	<a href="/connections" class="cta-button">Get Started</a>
</section>

<style>
	.cta-section {
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		gap: 8px;
		padding-top: 32px;
		margin-top: 4rem;
		margin-bottom: 5rem;
	}

	.cta-button {
		padding: 8px 16px;
		border-radius: 4px;
		font-size: 0.875rem;
		text-decoration: none;
		color: var(--text-link);
		border: 1px solid var(--text-link);
		width: fit-content;
		margin-top: 8px;
		transition: background 0.15s, color 0.15s;
	}

	.cta-button:hover {
		background: var(--text-link);
		color: var(--bg-primary);
		filter: none;
	}
</style>
